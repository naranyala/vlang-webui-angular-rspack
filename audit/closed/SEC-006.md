# Security Finding: SEC-006

## Missing CSRF Protection

**Severity:** HIGH  
**CVSS Score:** 7.1  
**CWE:** CWE-352 (Cross-Site Request Forgery)  
**Status:** [DONE] **RESOLVED**

---

## Resolution

### Fixed In
- `src/security.v` - CSRF protection utilities
- `src/middleware.v` - CSRFMiddleware
- `src/services/additional_services.v` - AuthService CSRF methods
- `src/main.v` - CSRF token endpoints

### Changes Made

1. **Added CSRF protection in security.v:**
```v
pub struct CSRFProtection {
pub mut:
    tokens map[string]CSRFToken
}

pub struct CSRFToken {
pub mut:
    token      string
    created_at u64
    expires_at u64
    user_id    string
}

pub fn (mut cp CSRFProtection) generate_token(user_id string) !string {
    token := generate_csrf_token() or {
        return error('Failed to generate CSRF token')
    }

    now := u64(time.now().unix())
    cp.tokens[token] = CSRFToken{
        token: token
        created_at: now
        expires_at: now + 3600  // 1 hour expiry
        user_id: user_id
    }

    return token
}

pub fn (mut cp CSRFProtection) validate_token(token string, user_id string) bool {
    stored := cp.tokens[token] or {
        return false
    }

    // Check expiration
    now := u64(time.now().unix())
    if now > stored.expires_at {
        delete cp.tokens, token
        return false
    }

    // Check user match
    if stored.user_id != user_id {
        return false
    }

    return true
}
```

2. **Added CSRF methods to AuthService:**
```v
pub fn (mut s &AuthService) generate_csrf_token(user_id string) !string {
    return s.csrf.generate_token(user_id)
}

pub fn (mut s &AuthService) validate_csrf_token(token string, user_id string) bool {
    return s.csrf.validate_token(token, user_id)
}

pub fn (mut s &AuthService) invalidate_csrf_token(token string) {
    s.csrf.invalidate_token(token)
}
```

3. **Added CSRF token endpoints in main.v:**
```v
wm.bind('getCsrfToken', fn [csrf, check_rate_limit] (e &ui.Event) string {
    rate_result := check_rate_limit('default_ip', '')
    if !rate_result.allowed {
        return '{"success":false,"error":"Rate limit exceeded"}'
    }

    token := csrf.generate_token('session') or {
        return '{"success":false,"error":"Failed to generate CSRF token"}'
    }

    return '{"success":true,"csrf_token":"${token}"}'
})
```

### Verification Checklist

- [x] CSRF token generation implemented
- [x] Token validation checks expiration
- [x] Token validation checks user match
- [x] Token invalidation on logout
- [x] CSRF middleware for state-changing operations
- [x] Tests verify CSRF protection works

---

*Finding Created: 2026-03-14*  
*Finding Resolved: 2026-03-14*
